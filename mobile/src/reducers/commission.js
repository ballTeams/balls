

const initState = {
   
};

const commission = (state = initState, action) => {
    switch (action.type) {
        
        default:
            return initState;
    }
};

export default commission;